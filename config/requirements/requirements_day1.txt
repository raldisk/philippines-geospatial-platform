# Day 1 — PH Geospatial Platform v2.2 — Archive inspection + Bronze write

# Geospatial inspection (fiona-only — avoids GEOS in loop per ADR-006)
fiona>=1.9.0
pyproj>=3.6.0
shapely>=2.0.0

# Data
pandas>=2.0.0
pyarrow>=14.0.0

# Structured logging
structlog>=24.2.0

# Testing
pytest>=7.0.0
