# Day 2 — PH Geospatial Platform v2.2
# Install: pip install -r requirements_day2.txt

# Geospatial core
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0

# H3 hexagonal grid (ADR-003, ADR-009)
h3>=3.7.7

# Jenks natural breaks classification (Section 7.3)
mapclassify>=2.6.0

# Data
pandas>=2.0.0
numpy>=1.26.0

# Structured logging (Section 8.1)
structlog>=24.1.0

# Parquet I/O (GeoParquet with bbox coverage — ADR-012)
pyarrow>=14.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# tippecanoe + pmtiles: system binaries — NOT pip packages
# Install via:
#   tippecanoe : see day-2-README.md § Prerequisites
#   pmtiles CLI: go install github.com/protomaps/go-pmtiles@latest
