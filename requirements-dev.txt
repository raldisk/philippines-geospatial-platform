# Dev / test dependencies — not installed in production image
# Installed in CI alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Test runner
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# HTTP test client (FastAPI integration tests)
httpx>=0.27.0

# MinIO / S3 integration tests
boto3>=1.34.0

# Load / SLO testing
locust>=2.28.0

# Type stubs (mypy needs these)
types-requests>=2.32.0
