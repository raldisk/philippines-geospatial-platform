# PH Geospatial Platform v2.2 — Consolidated requirements
# Sources: requirements_day1.txt (generated), requirements_day2.txt, requirements.txt (Day 3)
# Rule 4: merged, deduped, sorted, boto3 + pytest added

# AWS / MinIO client (integration tests — FIX Rule 4)
boto3>=1.28.0

# DuckDB with spatial extension (bundled from 0.10+)
duckdb>=1.0.0

# FastAPI serving stack
fastapi>=0.111.0
httpx>=0.27.0
python-multipart>=0.0.9
slowapi>=0.1.9
uvicorn[standard]>=0.30.1

# Geospatial core
fiona>=1.9.0
geopandas>=0.14.0
h3>=3.7.7
mapclassify>=2.6.0
pyproj>=3.6.0
shapely>=2.0.0

# Data
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0

# Pydantic (FastAPI dependency — explicit pin)
pydantic>=2.7.4

# Structured logging
structlog>=24.2.0

# Testing
pytest>=7.0.0
pytest-cov>=5.0.0

# Visualization (Day 4 render script)
matplotlib>=3.8.0
Pillow>=10.0.0

# Cryptography (Airflow Fernet key generation)
cryptography>=42.0.0

# tippecanoe + pmtiles: system binaries — NOT pip packages
# tippecanoe: https://github.com/felt/tippecanoe/releases
# pmtiles CLI: go install github.com/protomaps/go-pmtiles@latest

# Retry / backoff (Action 3 — DuckDB connection resilience)
tenacity>=8.2.0

# Prometheus metrics (Action 4 — /metrics endpoint)
prometheus-client>=0.20.0
